import { describe, it, expect } from "@jest/globals";
import { Deck } from "../../src/model/deck";

describe("Function update()",()=>{
  // Branches
    // Applies to Deck
      // Wait to shuffle true
        // need a cleanup
        // don't need a cleanup
      // Wait to shuffle false
    // Does not apply to deck
      // Consecutive Treasure Plays
      // Not Consecutive Treasure Plays
  it("should process correctly",()=>{
    
  })
})
